#include <stdlib.h>
#include <stdio.h>
#include <math.h>
int main()
{


 double x,y;


 printf("Enter x:");
 scanf("%lf", &x);

   if( -5 <= x && x < -1)
      {
          y=exp(x-1);
      }
   else if ( 1 <= x && x < 5 )
      {
          y=exp(x+1);
      }


    else if ( 11 <= x && x < 15 )
      {
          y = -log (x) + x/cos(1*x)+1;
      }

       else
      {
        y = 5;
      }

    system("cls");
        printf("x = %lf", x);
         printf("\ny = %lf", y);
 return 0;
}
